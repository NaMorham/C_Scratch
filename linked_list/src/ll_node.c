#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

#include <Utils.h>
#include <ll_node.h>

struct LLNode_t* init_ll_node(struct LLNode_t* pNode, char *name)
{
	return pNode;
}

void dump_node(struct LLNode_t* pNode)
{

}